# 🔑 API 키 오류 해결 가이드

## ❌ 발견된 문제

스크린샷에서 확인한 문제:
- **API 키가 "Alza"로 시작함** → 올바른 형식은 **"AIza"**입니다!

## ✅ 해결 방법

### 1단계: API 키 다시 복사

1. **Google AI Studio 접속**
   - https://aistudio.google.com/app/apikey
   - 또는 현재 열려있는 Google AI Studio 페이지에서

2. **API 키 세부정보 확인**
   - "budget management 1207" 키 클릭
   - "API 키" 필드에서 전체 키 확인
   - **"AIza"로 시작하는지 확인** (대문자 I, 소문자 za)

3. **키 복사**
   - "키 복사" 버튼 클릭
   - 또는 키 전체를 드래그하여 복사
   - **주의**: 복사할 때 앞뒤 공백이 포함되지 않도록 주의

### 2단계: 웹 설정에 올바른 키 입력

1. **설정 페이지 접속**
   - http://localhost:3000/settings
   - (JavaScript chunk 오류가 있으면 먼저 서버 재시작 필요)

2. **AI 설정 섹션 찾기**
   - "AI 설정 (Gemini API)" 섹션
   - "Gemini API 키" 입력 필드

3. **키 입력**
   - 기존 키 삭제
   - 새로 복사한 키 붙여넣기
   - **"AIza"로 시작하는지 확인**

4. **저장**
   - 입력 필드에서 포커스가 벗어나면 자동 저장됨
   - 또는 페이지 새로고침 전에 확인

### 3단계: JavaScript Chunk 오류 해결

**오류 메시지**:
```
Loading chunk vendors-node_modules_firebase_firestore_dist_index_cjs_js-node_modules_mui_icons-material_esm-0db6f4 failed.
```

**해결 방법**:

#### 방법 1: 개발 서버 재시작 (권장)

1. **터미널에서 서버 중지**
   - 터미널 창 찾기
   - `Ctrl + C` 누르기

2. **서버 다시 시작**
   ```bash
   cd "/Volumes/Samsung USB/budget_management_anti/frontend"
   npm start
   ```

3. **브라우저 새로고침**
   - `Ctrl + Shift + R` (하드 리프레시)
   - 또는 `Ctrl + F5`

#### 방법 2: 브라우저 캐시 완전 삭제

1. **Chrome 개발자 도구 열기**
   - `F12` 또는 `Ctrl + Shift + I`

2. **Application 탭 열기**
   - "Application" 탭 클릭
   - 왼쪽 사이드바에서 "Storage" 확장
   - "Clear site data" 클릭
   - "Clear site data" 버튼 클릭

3. **또는 간단하게**
   - `Ctrl + Shift + Delete`
   - "캐시된 이미지 및 파일" 선택
   - "데이터 삭제" 클릭

### 4단계: API 테스트

1. **설정 페이지에서 테스트**
   - http://localhost:3000/settings
   - "AI 설정 (Gemini API)" 섹션
   - "API 테스트" 버튼 클릭
   - 결과 확인

2. **또는 테스트 페이지에서**
   - http://localhost:3000/test-integration.html
   - "Gemini API 테스트 실행" 버튼 클릭
   - 결과 확인

## 🔍 API 키 형식 확인

올바른 Gemini API 키 형식:
- ✅ **"AIza"**로 시작 (대문자 A, 대문자 I, 소문자 za)
- ✅ 길이: 약 39자
- ✅ 예시: `AIzaSyA8ljeKPN_h8Y6WAGwPiYE9IfxPHa9yY2U`

잘못된 형식:
- ❌ "Alza"로 시작 (대문자 A, 소문자 lza) - **오타!**
- ❌ "AIza"가 아닌 다른 문자로 시작
- ❌ 공백 포함
- ❌ 특수문자 잘못 포함

## 📋 체크리스트

다음 항목들을 확인하세요:

- [ ] Google AI Studio에서 API 키가 **"AIza"**로 시작하는지 확인
- [ ] API 키를 정확히 복사했는지 확인 (공백 없이)
- [ ] 웹 설정에 올바른 키 입력됨
- [ ] 개발 서버 재시작함
- [ ] 브라우저 캐시 클리어함
- [ ] 페이지 새로고침함
- [ ] API 테스트 성공함

## 🚨 여전히 문제가 있으면

1. **브라우저 콘솔 확인** (F12 > Console)
   - 오류 메시지 확인

2. **네트워크 탭 확인** (F12 > Network)
   - API 요청이 실패하는지 확인
   - 응답 코드 확인 (200, 401, 403 등)

3. **Google Cloud Console에서 Gemini API 활성화 확인**
   - https://console.cloud.google.com/apis/library?project=budget-management-478706
   - "Gemini API" 검색
   - "사용 중지" 버튼이 보이면 활성화됨

4. **Google AI Studio에서 키 활성화 확인**
   - https://aistudio.google.com/app/apikey
   - 키 옆에 토글 스위치가 "켜기" 상태인지 확인


